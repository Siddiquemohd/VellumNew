import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};
module.exports = {
  images: {
    domains: ['vallum.in'], // Add the external domain here
  },
};


export default nextConfig;
